heart-failure-prediction/
├── app/
│   └── app.py                  ← My Streamlit app (the main thing people will use)
├── data/
│   └── heart_failure_clinical_records_dataset.csv   ← The dataset
├── notebooks/              ← My exploration & analysis and Where you train & test models
│             
├── models/                 (optional but recommended)
│   └── best_model.pkl      ← Saved my trained model here later
├── requirements.txt        ← List of packages needed to run the project
├── .gitignore              ← Tells Git what to ignore
└── README.md               ← The most important file — explains everything
